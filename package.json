{
  "name": "universal-proxy",
  "version": "1.0.0",
  "description": "HTTP + WebSocket proxy (suitable for render.com)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "express-rate-limit": "^6.8.0",
    "ws": "^8.13.0"
  },
  "engines": {
    "node": ">=18"
  }
}
